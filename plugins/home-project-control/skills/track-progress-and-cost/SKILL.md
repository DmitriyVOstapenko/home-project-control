---
name: track-progress-and-cost
description: Сопоставляет план, фактический ход работ, замечания, договорные этапы, счета и подтверждённые платежи; создаёт локальный отчёт о состоянии проекта. Использовать для еженедельного контроля, подготовки статуса, проверки запрошенной подрядчиком оплаты, анализа отклонений бюджета и сроков либо сверки расходов с документами.
---

# Ход работ и расходы

## Входной контроль проекта

До чтения документов вызвать `inspect_project_workspace` с точным путём текущей рабочей папки. При `existing_project_ready` продолжить без вопросов. Иначе открыть `open_project_setup_form` и не начинать профильную работу до `gatePassed: true`; создание и восстановление структуры передать проводнику проекта. При смене папки проверить её заново.

## Интерактивные вопросы

Когда пользователь выбирает один или несколько готовых вариантов, вызывать `open_choice_form`: `single` для одного ответа, `multiple` для нескольких. Свободный текст оставлять для уникальных фактов и пояснений. Выбор считать подтверждённым только после `submit_choice_form` с `accepted: true`; форма не заменяет подтверждающего документа и отдельного согласия на запись. Если форма недоступна, дать короткие нумерованные варианты в чате.

## Источники

Использовать реестры `.home-control/work_items.csv`, `costs.csv`, `issues.csv` и `documents.json`. Если существуют, также учитывать `changes.csv`, `commitments.csv`, `acceptance.csv` и `procurement.csv`. Схемы и статусы читать в [tracking-schema.md](references/tracking-schema.md).

## Правила прогресса

- Отчёт подрядчика является заявлением подрядчика.
- Фото подтверждает видимое состояние на указанную дату, но не качество скрытых работ.
- Акт подтверждает оформленную сторонами приёмку в пределах акта.
- Процент готовности без измеримого основания помечать как заявленный.
- Завершение скрываемого этапа не подтверждать без предусмотренной проектом фиксации или приёмки.
- Не выводить просрочку, если нет подтверждённой плановой даты и зависимости.
- Не приравнивать физическую готовность, договорное выполнение, приёмку и возможность оплаты: показывать эти состояния раздельно.
- При изменении базового объёма сохранять исходный план и связывать отклонение с утверждённым либо неутверждённым изменением.

## Правила расходов

- Суммировать как подтверждённые фактические расходы только строки `confirmed_paid` с действующим `evidence_document_id` и локатором.
- Показывать предложения, счета, заявленные и неподтверждённые суммы отдельно.
- Группировать итоги по валюте; не конвертировать без согласованного курса.
- Не считать платежом акт, счёт или коммерческое предложение.
- При расхождении реестра и документа показать конфликт.

## Отчёт

После обновления реестров использовать:

```text
python scripts/build_dashboard.py <папка-проекта>
```

Скрипт создаёт `.home-control/reports/project-status.md`, не изменяя исходные документы. В выводе дополнительно объяснить наиболее существенные отклонения и указать документы, на которых основан вывод.

Отчёт должен отдельно показывать: просроченные подтверждённые обязательства, предъявленные к приёмке этапы, открытые замечания, неутверждённые изменения с влиянием на цену, ближайшие закупки и платежи без достаточного подтверждения.
